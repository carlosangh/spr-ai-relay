services:
  - type: web
    name: spr-ai-relay
    env: python
    plan: starter
    buildCommand: docker build -t spr-ai-relay .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
      - key: ANTHROPIC_API_KEY
